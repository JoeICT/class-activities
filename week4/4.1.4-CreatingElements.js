// create a new unordered list (ul) element
const new_Unordered_List = document.createElement("ul");
// remove the paragraph element in the nav-bar
let childToRemove = document.querySelector(".nav-bar > p");
childToRemove

// add your new ul element to the nav-bar
const unordered = document.createElement("ul");

document.querySelector(".nav-bar").append(unordered);

// append the new element as the last child element to 'main'

document.querySelector("main").appendChild().lastChild;

// create two new list item (li) elements, and add some text to them

const list = document.createElement("li");
const list1 = document.createElement("li");

listItem1.textContent = "1st new list item";
listItem2.textContent = "1st new list item";

// add the li elements to the ul in the nav-bar

